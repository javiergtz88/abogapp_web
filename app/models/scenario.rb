class Scenario
  def initialize(user)
    # gets the user object reference
    @user = user
  end

  def search(query)
    # search the query in the knowledge base
  end

  def get(id)
    # get the article with id
    # format return values according to user membership type
  end
end
